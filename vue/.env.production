# 页面标题
VITE_APP_TITLE = 后台管理端

# 生产环境配置
VITE_APP_ENV = 'production'

# 默认用于本机打包验证。局域网打包前通过环境变量覆盖为本机局域网 IP：
# $env:VITE_APP_BASE_API='http://192.168.1.100:8080'; npm run build:prod
VITE_APP_BASE_API = 'http://localhost:8080'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
